using System.Windows;
using System.Windows.Input;

namespace MouseMove
{
    // nuttig voor oefening 3.6 : bepaling van de positie van de muis bij bewegen over het canvas (canvas moet een achtergrondkleur hebben!!!!!)
    // 1. tov linkerbovenhoek van het canvas
    // 2. tov linkerbovenhoek van de mainwindow

    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();
        }

    }
}
